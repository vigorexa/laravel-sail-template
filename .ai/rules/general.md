---
paths:
  - '**/*.php'
---

# General

## declare(strict_types=1) in every PHP file
Every PHP file in this project starts with `declare(strict_types=1);` immediately after the opening tag. Add it to new PHP files; do not omit it in app code, modules, config, routes, or tests.
